# Examples

This folder is reserved for synthetic examples or public datasets with clear reuse permission.

Do not place patient data, collaborator data, raw clinical sequencing files, or internal project outputs here.

Suggested future examples:

- a tiny synthetic BAM inspection walkthrough;
- a public modified-base BAM example if reuse terms are clear;
- expected output snippets from `samtools flagstat` and `modkit pileup`;
- toy CpG methylation BED rows for parsing practice.

